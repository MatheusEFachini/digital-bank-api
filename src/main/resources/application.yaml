spring:
  application:
    name: digital-bank-api

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/digital_bank
    username: admin
    password: 123

  jpa:
    show-sql: true

  flyway:
    enabled: true
server:
  port: 8080